<!doctype html>
<html lang="es">
    <head>
        <meta charset="utf-8">
        <title>#(title)</title>
    </head>

    <body>
        <h1>#(title)</h1>
        <p>#(time)</p>
        #if(weekday == true):
        <p>#uppercased("Weekend time!")</p>
        #else:
        <p>Working ...</p>
        #endif
    </body>
</html>
